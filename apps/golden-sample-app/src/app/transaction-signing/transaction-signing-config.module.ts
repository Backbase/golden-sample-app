import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Configuration module for transaction signing.
 *
 * This module provides configuration settings for transaction signing functionality.
 * Currently it's a placeholder since transaction signing is implemented as a stub.
 */
@NgModule({
  imports: [CommonModule],
  providers: [
    // Configuration providers would go here
  ],
})
export class TransactionSigningConfigModule {}
