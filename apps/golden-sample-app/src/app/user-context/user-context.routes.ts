import { Routes } from '@angular/router';
import { UserContextComponent } from './user-context.component';

export const userContextRoutes: Routes = [
  {
    path: '',
    component: UserContextComponent,
  },
];
